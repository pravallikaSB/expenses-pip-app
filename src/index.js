import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

/* App is a component */
/* in the place of the element root div, we replace 
App component in it */
ReactDOM.render(<App></App>, document.getElementById('root'));

